<!-- views/admin.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      overflow: hidden;
      position: relative;
      background-color: #001f3f; /* Dark blue background color */
    }

    h1 {
      text-align: center;
      color: #3498db;
      margin-top: 100px; /* Adjust as needed */
    }

    form {
      max-width: 400px;
      margin: 0 auto;
      text-align: center;
      z-index: 1;
    }

    input {
      width: 100%;
      padding: 10px;
      margin-bottom: 10px;
      box-sizing: border-box;
    }

    button {
      background-color: #3498db;
      color: #fff;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    button:hover {
      background-color: #2980b9;
    }

    .user-link {
      display: block;
      text-align: center;
      margin-top: 20px;
      font-size: 16px;
      color: #3498db;
      text-decoration: none;
      transition: color 0.3s;
      z-index: 100000;
      position: absolute;
      left: 47%;
    }

    .user-link:hover {
      color: #2980b9;
    }

    /* Background Animation */
    .stars {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 0;
    }

    .star {
      position: absolute;
      background: #fff;
      border-radius: 50%;
      animation: blink 2s infinite;
    }

    .star:nth-child(1) {
      top: 10%;
      left: 10%;
      width: 6px;
      height: 6px;
    }

    .star:nth-child(2) {
      top: 20%;
      left: 80%;
      width: 4px;
      height: 4px;
      animation-delay: 1s;
    }

    /* Additional Stars */
    .star:nth-child(3),
    .star:nth-child(4),
    .star:nth-child(5),
    .star:nth-child(6) {
      top: 30%;
      left: 40%;
      width: 3px;
      height: 3px;
      animation-delay: 0.5s;
    }

    /* Moon Effect */
    .moon {
      position: absolute;
      top: 10%;
      right: 10%;
      transform: translate(-50%, -50%);
      background: #f5f5f5;
      border-radius: 50%;
      width: 100px;
      height: 100px;
      box-shadow: 0 0 30px #fff;
      z-index: 1;
    }

    @keyframes blink {
      50% {
        opacity: 0.5;
      }
    }
  </style>
</head>
<body>
  <div class="stars">
    <div class="star"></div>
    <div class="star"></div>
    <div class="star"></div>
    <div class="star"></div>
    <!-- Add more stars here -->
  </div>

  <div class="moon"></div>

  <h1>Admin Dashboard</h1>
  <form action="/upload" method="post" enctype="multipart/form-data">
    <input type="file" name="image" accept="image/*" required>
    <input type="text" name="label" placeholder="Image Label" required>
    <button type="submit">Upload</button>
  </form>

  <a href="/" class="user-link">View User Page</a>

</body>
</html>
