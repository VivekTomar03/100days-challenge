<!-- views/index.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User View</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      overflow: hidden;
      position: relative;
      background-color: #001f3f; /* Dark blue background color */
    }

    h1 {
      text-align: center;
      color: #3498db;
      margin-top: 100px; /* Adjust as needed */
    }

    form {
      max-width: 400px;
      margin: 0 auto;
      text-align: center;
      z-index: 1;
    }

    input {
      width: 100%;
      padding: 10px;
      margin-bottom: 10px;
      box-sizing: border-box;
    }

    button {
      background-color: #3498db;
      color: #fff;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    button:hover {
      background-color: #2980b9;
    }

    .user-link {
      display: block;
      text-align: center;
      margin-top: 20px;
      font-size: 16px;
      color: #3498db;
      text-decoration: none;
      transition: color 0.3s;
      z-index: 100000;
      position: absolute;
      left: 47%;
    }

    .user-link:hover {
      color: #2980b9;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
    }

    .admin-link {
      padding: 10px 20px;
      background-color: #3498db;
      color: #fff;
      text-decoration: none;
      border-radius: 5px;
      font-weight: bold;
      transition: background-color 0.3s;
      z-index: 100000;
      display: flex;
      justify-content: center;
    }

    .admin-link:hover {
      background-color: #2980b9;
    }

    ul {
      list-style: none;
      padding: 0;
      display: flex;
      flex-wrap: wrap;
      justify-content: space-around;
    }

    li {
      width: calc(33.33% - 20px);
      margin: 10px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      overflow: hidden;
      border-radius: 8px;
      transition: transform 0.3s;
    }

    li:hover {
      transform: scale(1.05);
    }

    img {
      width: 100%;
      height: 150px; /* Adjust the height as needed */
      object-fit: cover;
      border-radius: 8px 8px 0 0;
    }

    p {
      padding: 10px;
      margin: 0;
      text-align: center;
      color: #fff;
    }

        /* Background Animation */
        .stars {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 0;
    }

    .star {
      position: absolute;
      background: #fff;
      border-radius: 50%;
      animation: blink 2s infinite;
    }

    .star:nth-child(1) {
      top: 10%;
      left: 10%;
      width: 6px;
      height: 6px;
    }

    .star:nth-child(2) {
      top: 20%;
      left: 80%;
      width: 4px;
      height: 4px;
      animation-delay: 1s;
    }

    /* Additional Stars */
    .star:nth-child(3),
    .star:nth-child(4),
    .star:nth-child(5),
    .star:nth-child(6) {
      top: 30%;
      left: 40%;
      width: 3px;
      height: 3px;
      animation-delay: 0.5s;
    }

    /* Moon Effect */
    .moon {
      position: absolute;
      top: 10%;
      right: 10%;
      transform: translate(-50%, -50%);
      background: #f5f5f5;
      border-radius: 50%;
      width: 100px;
      height: 100px;
      box-shadow: 0 0 30px #fff;
      z-index: 1;
    }

    @keyframes blink {
      50% {
        opacity: 0.5;
      }
    }
  </style>
</head>
<body>
    <div class="stars">
        <div class="star"></div>
        <div class="star"></div>
        <div class="star"></div>
        <div class="star"></div>
        <!-- Add more stars here -->
      </div>
    
     
  <div class="header">
    <h1>User View</h1>
    <a href="/admin" class="admin-link">Visit Admin Page</a>
  </div>
  <div class="moon"></div>
  <% if (imageData.length > 0) { %>
    <ul>
      <% imageData.forEach(image => { %>
        <li>
          <img src="/images/<%= image.filename %>" alt="<%= image.label %>">
          <p><strong>Label:</strong> <%= image.label %></p>
        </li>
      <% }); %>
    </ul>
  <% } else { %>
    <p>No images available.</p>
  <% } %>

</body>
</html>
